# ── 构建产物与中间物 ──────────────────────────────
app/out/
probe_audio/out/
app/build.log
app/build.err.log
probe_audio/build.log
probe_audio/build.err.log
*.apk
*.dex
*.unsigned.apk
*.aligned.apk

# ── 签名密钥（build.ps1 缺失时会自动 keytool 生成）──
debug.keystore

# ── 第三方素材与反编译分析产物（体积大 / 版权受限，不分发）──
#   references/NX_emotion-ball/  表情球上游（自带"禁止商用"许可，需自行 clone）
#   references/emotion-ball/     旧版参照
#   references/xiaobu/           OPPO 专有固件 APK 及反编译源码 —— 绝不可再分发
references/
probe_audio/pulled/

# ── 本地设备身份备份（含真实 Device-Id/Client-Id/激活码，仅本机保留）──
tools/config.pinned.json

# ── Agent 会话内部笔记（非项目内容）──
.workbuddy/
.context/
*.log

# ── 杂项 ──
__pycache__/
.DS_Store
Thumbs.db

# ── 内部交接文档（本地保留，不发布）──
HANDOFF.md
PLAN.md
PINNED_IDENTITY.md
